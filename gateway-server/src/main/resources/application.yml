server:
  port: 8882
spring:
  profiles:
    include: common
  application:
    name: gateway-server
  datasource:
    url: jdbc:mysql://localhost:3306/sca-gateway?characterEncoding=utf8&serverTimezone=GMT%2b8
    username: root
    password: 123456
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true